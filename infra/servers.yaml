#
#
#
- name: configure consul servers
  hosts: consul_servers
  become: yes
  roles:
    - common
    - node_exporter
    - consul

- name: configure prometheus servers
  hosts: prom_servers
  become: yes
  roles:
    - common
    - alertmanager
    - node_exporter
    - prometheus
    - grafana
    - nginx
